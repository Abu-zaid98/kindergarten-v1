# 🎓 نظام إدارة الدفع الشهري للروضة
## Kindergarten Monthly Payment Management System

---

## 📋 نظرة عامة على المشروع

نظام ويب تقدمي (PWA) بسيط وسهل الاستخدام لإدارة الدفع الشهري لطلاب الروضة،
يتيح تتبع حالة الدفع لكل طالب، وعرض إحصائيات مالية شهرية وسنوية.

---

## 🛠️ المكدس التقني (Tech Stack)

| الطبقة | التقنية |
|--------|---------|
| الواجهة الأمامية | React 19 |
| التصميم | Tailwind CSS 4 |
| قاعدة البيانات المحلية | IndexedDB (عبر Dexie.js أو idb) |
| PWA | Vite PWA Plugin + Service Worker |
| التوجيه | React Router v7 |
| الحالة العامة | Zustand أو Context API |
| الأيقونات | Lucide React |
| الخط | Tajawal أو Cairo (Google Fonts) |
| العملة | شيكل إسرائيلي جديد (₪ / ILS) |
| الاتجاه | RTL (يمين إلى يسار) |
| تصدير Excel | SheetJS (xlsx) |
| الطباعة | CSS Print Styles + html2canvas (اختياري) |

---

## 🔐 نظام المصادقة

### أول تشغيل للتطبيق
- يظهر شاشة إعداد كلمة المرور (Setup Screen)
- المستخدم يدخل كلمة مرور من اختياره وتأكيدها
- يتم تخزين كلمة المرور بشكل مشفر (SHA-256 أو bcrypt-js) في:
  - `localStorage` عند الاستخدام كموقع ويب
  - ملفات التطبيق المحلية عند تثبيته كـ PWA
- بعد الإعداد، يدخل المستخدم مباشرة للتطبيق

### تسجيل الدخول
- شاشة إدخال كلمة المرور في كل جلسة
- خيار "تذكرني لـ 7 أيام" (اختياري)
- زر "نسيت كلمة المرور؟" → يطلب تأكيداً بالضغط ثم يسمح بإعادة تعيينها (مع تحذير أن البيانات ستبقى)

### إعادة تعيين كلمة المرور
- من داخل الإعدادات: إدخال القديمة ثم الجديدة
- من شاشة تسجيل الدخول: زر "إعادة تعيين" مع تأكيد الإجراء

---

## 📱 صفحات التطبيق (Screens / Pages)

### 1. 🔒 شاشة تسجيل الدخول / الإعداد الأول
- إعداد كلمة المرور (أول مرة)
- تسجيل الدخول (كل مرة لاحقة)
- إعادة تعيين كلمة المرور

---

### 2. 🏠 لوحة التحكم الرئيسية (Dashboard)
عرض إحصائيات الشهر الحالي دفعة واحدة:

| البطاقة | المحتوى |
|---------|---------|
| 💰 إجمالي المحصّل | مجموع المدفوعات للشهر الحالي (₪) |
| 👦 إجمالي الطلاب | العدد الكلي للطلاب المسجلين |
| 📋 إجمالي المبلغ المطلوب | عدد الطلاب × الرسوم الشهرية |
| ⚠️ العجز | الفرق بين المطلوب والمحصّل |
| ✅ دفعوا | عدد الطلاب الذين أتموا الدفع |
| ❌ لم يدفعوا | عدد الطلاب المتبقين مع أسمائهم |

**فلاتر سريعة:**
- اختيار الشهر / السنة
- عرض الطلاب غير المدفوعين فقط

---

### 3. 👧 إدارة الطلاب (Students Management)
قائمة جميع الطلاب مع:
- بحث بالاسم أو اسم ولي الأمر
- إضافة طالب جديد
- تعديل بيانات طالب
- حذف طالب (مع تأكيد)
- عرض بطاقة الطالب

**بيانات كل طالب:**

```
الاسم الكامل للطالب *
تاريخ الميلاد
الجنس (ذكر / أنثى)
الصف / الفصل (اختياري)
ملاحظات خاصة

--- بيانات ولي الأمر ---
اسم ولي الأمر *
صلة القرابة (أب / أم / جد / غيره)
رقم الجوال الأساسي *
رقم الجوال الثانوي
البريد الإلكتروني

--- بيانات الرسوم ---
الرسوم الشهرية المخصصة (₪) *  ← قد تختلف من طالب لآخر
تاريخ التسجيل
الحالة (نشط / غير نشط)
```

---

### 4. 💳 تتبع المدفوعات (Payments Tracker)

**الواجهة:**
- جدول / قائمة بكل الطلاب للشهر المختار
- لكل طالب: الاسم، المبلغ المطلوب، حالة الدفع، طريقة الدفع، التاريخ

**حالات الدفع:**
| الحالة | اللون |
|--------|-------|
| ✅ تم الدفع | أخضر |
| ⏳ دفع جزئي | أصفر |
| ❌ لم يدفع | أحمر |

**طرق الدفع المتاحة:**
- 💵 كاش (نقداً)
- 👛 محفظة إلكترونية
- 🏦 تحويل بنكي
- 📱 جوال باي (Jawwal Pay)
- 💙 بال باي (PalPay)
- 🔄 أخرى (مع حقل نصي)

**إجراءات على كل طالب:**
- تغيير حالة الدفع (زر واضح كبير)
- إدخال المبلغ المدفوع + طريقة الدفع + تاريخه
- إضافة ملاحظة على الدفعة
- عرض سجل مدفوعات الطالب

---

### 5. 📊 الإحصائيات والتقارير (Reports)

**تقرير شهري:**
- إجمالي المحصّل vs المطلوب (شريط تقدم)
- نسبة التحصيل (%)
- توزيع طرق الدفع (رسم بياني)
- قائمة غير المدفوعين

**تقرير سنوي:**
- رسم بياني للتحصيل شهراً بشهر (12 شهر)
- أعلى شهر تحصيلاً وأدناه
- إجمالي التحصيل السنوي

**تقرير يومي:**
- المدفوعات المسجلة اليوم
- مجموع اليوم

---

### 6. 📤 التصدير والطباعة (Export & Print)

#### تصدير Excel (xlsx)
يتم التصدير باستخدام مكتبة **SheetJS** مباشرة في المتصفح بدون خادم.

**التقارير القابلة للتصدير:**

| التقرير | محتوى الملف |
|---------|------------|
| كشف المدفوعات الشهري | اسم الطالب، المبلغ المطلوب، المدفوع، الحالة، الطريقة، التاريخ، الملاحظة |
| قائمة غير المدفوعين | اسم الطالب، رقم ولي الأمر، المبلغ المطلوب |
| التقرير السنوي | شهر بشهر: المحصّل، المطلوب، العجز، نسبة التحصيل |
| سجل طالب | كل مدفوعاته منذ التسجيل |
| قائمة الطلاب الكاملة | بيانات جميع الطلاب وأولياء أمورهم |

**تنسيق ملف Excel:**
- اسم الملف يتضمن اسم الروضة + الشهر/السنة تلقائياً
  - مثال: `روضة_النور_كشف_أكتوبر_2026.xlsx`
- **ورقة عمل أولى:** ملخص (اسم الروضة، الشهر، الإجمالي، عدد الطلاب)
- **ورقة عمل ثانية:** بيانات التفصيلية
- الخلايا تدعم RTL والخط العربي
- الأرقام بتنسيق العملة (₪)
- الصفوف الملونة: أخضر للمدفوع، أحمر لغير المدفوع، أصفر للجزئي

**زر التصدير:**
- يظهر في صفحة التقارير وصفحة المدفوعات
- أيقونة واضحة 📥 مع نص "تصدير Excel"
- عند الضغط: تحميل فوري بدون نافذة إضافية

---

#### الطباعة (Print)
نظام طباعة مدمج عبر `window.print()` مع CSS مخصص لكل تقرير.

**التقارير القابلة للطباعة:**

| التقرير | التنسيق |
|---------|---------|
| كشف حضور/دفع شهري | جدول A4 بجميع الطلاب مع خانة التوقيع |
| إيصال دفع فردي | بطاقة صغيرة تُطبع كإيصال للولي |
| قائمة المتأخرين | قائمة مرتبة بالأسماء والمبالغ |
| ملخص شهري | صفحة واحدة بالإحصائيات الكاملة |

**تنسيق الطباعة:**
- رأس الصفحة: اسم الروضة + اسم التقرير + التاريخ
- تذييل الصفحة: رقم الصفحة + "سري - للاستخدام الداخلي"
- خط Tajawal/Cairo مضمّن ليظهر بشكل صحيح في الطباعة
- إخفاء عناصر التنقل والأزرار تلقائياً عند الطباعة (`@media print`)
- دعم الطباعة على A4 وA5
- اتجاه RTL في الجدول المطبوع

**زر الطباعة:**
- أيقونة واضحة 🖨️ مع نص "طباعة"
- معاينة ما قبل الطباعة (Print Preview الافتراضي للمتصفح)

---

**مكون `ExportToolbar`** (يُضاف في صفحات التقارير والمدفوعات):
```jsx
// شريط أدوات موحد يظهر في أعلى كل صفحة قابلة للتصدير
<ExportToolbar
  onExcelExport={() => exportToExcel(data, filename)}
  onPrint={() => window.print()}
  reportTitle="كشف المدفوعات - أكتوبر 2026"
/>
```

---

### 8. ⚙️ الإعدادات (Settings)
- **اسم الروضة** (يظهر في رأس الصفحة ورأس كل طباعة)
- **الرسوم الشهرية الافتراضية** (₪)
- **السنة الدراسية الحالية**
- **تغيير كلمة المرور**
- **تصدير البيانات** (JSON) للنسخ الاحتياطي
- **استيراد البيانات** (JSON) للاستعادة
- **حذف كل البيانات** (مع تأكيد صارم)

---

## 🗃️ هيكل قاعدة البيانات المحلية (IndexedDB)

### جدول: `students`
```js
{
  id: string,              // UUID
  fullName: string,        // الاسم الكامل للطالب
  birthDate: string,       // تاريخ الميلاد (ISO)
  gender: 'male' | 'female',
  classroom: string,       // الفصل (اختياري)
  notes: string,           // ملاحظات
  guardianName: string,    // اسم ولي الأمر
  guardianRelation: string,// صلة القرابة
  phone1: string,          // رقم جوال أساسي
  phone2: string,          // رقم جوال ثانوي
  email: string,
  monthlyFee: number,      // الرسوم الشهرية (₪)
  enrollmentDate: string,  // تاريخ التسجيل
  isActive: boolean,       // نشط أم لا
  createdAt: string,
  updatedAt: string
}
```

### جدول: `payments`
```js
{
  id: string,              // UUID
  studentId: string,       // مرجع لـ students.id
  month: number,           // الشهر (1-12)
  year: number,            // السنة
  status: 'paid' | 'partial' | 'unpaid',
  amountDue: number,       // المبلغ المطلوب (₪)
  amountPaid: number,      // المبلغ المدفوع (₪)
  paymentMethod: 'cash' | 'wallet' | 'bank' | 'jawwal_pay' | 'pal_pay' | 'other',
  paymentMethodNote: string, // تفاصيل إذا كانت "أخرى"
  paymentDate: string,     // تاريخ الدفع
  note: string,            // ملاحظة على الدفعة
  createdAt: string,
  updatedAt: string
}
```

### جدول: `settings`
```js
{
  id: 'app_settings',      // ثابت
  kindergartenName: string,
  defaultMonthlyFee: number,
  currentAcademicYear: string,
  passwordHash: string,    // كلمة المرور مشفرة
  rememberUntil: string    // تاريخ انتهاء "تذكرني"
}
```

---

## 🎨 إرشادات التصميم (Design Guidelines)

| العنصر | القيمة |
|--------|--------|
| الاتجاه | RTL (dir="rtl") |
| الخط | Tajawal أو Cairo (Google Fonts) |
| اللغة | العربية أساساً |
| العملة | شيكل (₪) |
| الألوان الأساسية | أبيض + أزرق هادئ + تمييز أخضر/أحمر |
| نمط التصميم | بسيط، مسطح، واسع، مريح للعين |
| المكونات | بطاقات واضحة، أزرار كبيرة، مسافات مريحة |
| الاستجابية | Mobile-first (الاستخدام الرئيسي على الجوال) |

---

## 📐 هيكل المشروع المقترح (Project Structure)

```
kindergarten-payment/
├── public/
│   ├── manifest.json          # PWA manifest
│   └── icons/                 # أيقونات PWA
├── src/
│   ├── components/
│   │   ├── layout/
│   │   │   ├── AppShell.jsx
│   │   │   ├── Navbar.jsx
│   │   │   └── BottomNav.jsx  # تنقل سفلي للجوال
│   │   ├── students/
│   │   │   ├── StudentCard.jsx
│   │   │   ├── StudentForm.jsx
│   │   │   └── StudentList.jsx
│   │   ├── payments/
│   │   │   ├── PaymentRow.jsx
│   │   │   ├── PaymentModal.jsx
│   │   │   └── PaymentBadge.jsx
│   │   ├── dashboard/
│   │   │   ├── StatsCard.jsx
│   │   │   └── UnpaidList.jsx
│   │   └── ui/
│   │       ├── Button.jsx
│   │       ├── Input.jsx
│   │       ├── Modal.jsx
│   │       └── SearchBar.jsx
│   ├── pages/
│   │   ├── LoginPage.jsx
│   │   ├── DashboardPage.jsx
│   │   ├── StudentsPage.jsx
│   │   ├── PaymentsPage.jsx
│   │   ├── ReportsPage.jsx
│   │   └── SettingsPage.jsx
│   ├── db/
│   │   ├── db.js              # إعداد Dexie.js / IndexedDB
│   │   ├── students.js        # عمليات CRUD للطلاب
│   │   └── payments.js        # عمليات CRUD للمدفوعات
│   ├── hooks/
│   │   ├── useStudents.js
│   │   ├── usePayments.js
│   │   └── useAuth.js
│   ├── store/
│   │   └── appStore.js        # Zustand store
│   │   ├── reports/
│   │   │   ├── ExportToolbar.jsx     # شريط أزرار التصدير والطباعة
│   │   │   └── PrintLayout.jsx       # تخطيط الطباعة (مخفي إلا عند print)
│   ├── utils/
│   │   ├── currency.js        # تنسيق الشيكل
│   │   ├── dates.js           # أسماء الشهور بالعربية
│   │   ├── crypto.js          # تشفير كلمة المرور
│   │   ├── export.js          # تصدير/استيراد JSON
│   │   ├── exportExcel.js     # تصدير Excel باستخدام SheetJS
│   │   └── printStyles.css    # CSS خاص بالطباعة (@media print)
│   ├── App.jsx
│   ├── main.jsx
│   └── index.css
├── vite.config.js             # إعداد Vite + PWA
├── tailwind.config.js
└── package.json
```

---

## 📦 المكتبات المطلوبة (Dependencies)

```json
{
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^7.0.0",
    "dexie": "^4.0.0",
    "dexie-react-hooks": "^1.1.7",
    "zustand": "^5.0.0",
    "lucide-react": "^0.400.0",
    "recharts": "^2.12.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "@vitejs/plugin-react": "^4.0.0",
    "vite-plugin-pwa": "^0.20.0",
    "tailwindcss": "^4.0.0",
    "@tailwindcss/vite": "^4.0.0"
  }
}
```

---

## 🚀 متطلبات التشغيل والبيئة

```bash
# تثبيت المشروع
npm create vite@latest kindergarten-payment -- --template react
cd kindergarten-payment
npm install

# تثبيت المكتبات
npm install dexie dexie-react-hooks zustand react-router-dom lucide-react recharts xlsx
npm install -D tailwindcss @tailwindcss/vite vite-plugin-pwa

# تشغيل بيئة التطوير
npm run dev

# بناء للإنتاج
npm run build
```

---

## ✅ قائمة المهام (Development Checklist)

### المرحلة 1 — البنية الأساسية
- [ ] إعداد Vite + React 19 + Tailwind CSS 4
- [ ] إعداد RTL و Tajawal/Cairo Font
- [ ] إعداد قاعدة البيانات (Dexie.js + IndexedDB)
- [ ] إعداد React Router
- [ ] إعداد Zustand Store

### المرحلة 2 — المصادقة
- [ ] شاشة إعداد كلمة المرور (أول مرة)
- [ ] شاشة تسجيل الدخول
- [ ] تشفير وحفظ كلمة المرور
- [ ] إعادة تعيين كلمة المرور

### المرحلة 3 — الطلاب
- [ ] صفحة قائمة الطلاب
- [ ] نموذج إضافة / تعديل طالب
- [ ] البحث والفلترة
- [ ] حذف طالب

### المرحلة 4 — المدفوعات
- [ ] صفحة تتبع المدفوعات
- [ ] تغيير حالة الدفع
- [ ] إدخال تفاصيل الدفعة (المبلغ، الطريقة، التاريخ)
- [ ] الفلترة بالشهر والسنة
- [ ] سجل مدفوعات الطالب

### المرحلة 5 — لوحة التحكم والإحصائيات
- [ ] بطاقات الإحصائيات الشهرية
- [ ] قائمة غير المدفوعين
- [ ] التقرير الشهري
- [ ] التقرير السنوي (رسم بياني)
- [ ] التقرير اليومي

### المرحلة 6 — التصدير والطباعة
- [ ] تثبيت وإعداد SheetJS (xlsx)
- [ ] `exportExcel.js` — دالة تصدير مشتركة لكل التقارير
- [ ] تصدير كشف المدفوعات الشهري (Excel)
- [ ] تصدير قائمة غير المدفوعين (Excel)
- [ ] تصدير التقرير السنوي (Excel)
- [ ] تصدير سجل طالب (Excel)
- [ ] تنسيق خلايا Excel: ألوان الحالة + RTL + العملة
- [ ] `printStyles.css` — تنسيق الطباعة (`@media print`)
- [ ] إخفاء عناصر التنقل عند الطباعة
- [ ] رأس وتذييل صفحة الطباعة (اسم الروضة، التاريخ، رقم الصفحة)
- [ ] طباعة كشف الدفع الشهري (جدول A4)
- [ ] طباعة إيصال دفع فردي
- [ ] مكون `ExportToolbar` موحد

### المرحلة 7 — الإعدادات
- [ ] اسم الروضة + الرسوم الافتراضية
- [ ] تغيير كلمة المرور
- [ ] تصدير البيانات (JSON)
- [ ] استيراد البيانات (JSON)
- [ ] حذف كل البيانات

### المرحلة 8 — PWA
- [ ] ملف `manifest.json`
- [ ] Service Worker (offline support)
- [ ] أيقونات PWA بمقاسات مختلفة
- [ ] اختبار التثبيت على الجوال
- [ ] التحقق من عمل تصدير Excel بعد التثبيت كـ PWA

---

## 🌐 ملاحظات مهمة للمطور

1. **RTL أولاً:** كل المكونات تُبنى بـ `dir="rtl"` من البداية، لا تعديل لاحقاً
2. **Mobile-first:** التصميم يبدأ للجوال ويتوسع للشاشات الكبيرة
3. **البيانات محلية فقط:** لا خوادم، لا إنترنت مطلوب للعمل (إلا لتحميل الخطوط)
4. **كلمة المرور:** لا يوجد استعادة من الخارج — التوثيق محلي بالكامل
5. **النسخ الاحتياطي:** يشجَّع المستخدم على تصدير البيانات بانتظام
6. **الشيكل:** استخدم `₪` + `toLocaleString('ar-IL')` لعرض الأرقام
7. **الشهور بالعربية:** يناير، فبراير... أو المحرم، صفر... (حسب السياق المدرسي)
8. **الأداء:** Dexie.js سريعة وخفيفة وتدعم PWA بشكل ممتاز
9. **التوافق:** يدعم Chrome, Firefox, Safari على iOS وAndroid
10. **SheetJS:** استخدم `xlsx.utils.book_new()` + `xlsx.utils.json_to_sheet()` + `xlsx.writeFile()` — يعمل 100% في المتصفح بدون خادم
11. **تنسيق Excel RTL:** أضف `ws['!cols']` لتحديد عرض الأعمدة، و`ws['!dir'] = 'rtl'` لضمان اتجاه الورقة
12. **الطباعة:** استخدم `@media print { .no-print { display: none } }` لإخفاء التنقل، و`@page { size: A4; margin: 1cm }` لضبط الصفحة
13. **إيصال الدفع:** صمّمه كـ `div` مخفي يظهر فقط عند الطباعة، بأبعاد إيصال A6 أو A5
14. **اسم ملف Excel:** `${kindergartenName}_${reportName}_${month}_${year}.xlsx` — استخدم اسم الروضة من الإعدادات

---

## 📄 الترخيص

مشروع خاص — جميع الحقوق محفوظة لصاحب الروضة.

---

*آخر تحديث: سبتمبر 2026 — أُضيف: تصدير Excel والطباعة*
